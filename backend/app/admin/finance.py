"""app.admin.finance — Sprint 21 C12C marker.

Admin finance domain: payments / billing / payouts — обслуживаются NestJS
payments-модулем (Stripe MOCKED). В server.py native endpoint'ов нет.

Файл-маркер на будущее: если появятся composition endpoint'ы поверх
Stripe + NestJS (например: aggregate revenue report, manual refund action),
их место здесь.
"""
